html
  head
    meta(charset='utf-8')
  body
    h1 
      a(href='/topic') Server Side JavsScript - Node.js
    ul
       each val in topics
        li 
          a(href='/topic/' + val) #{val}
    article
      h2 #{title}
      p #{desc}
    div
      a(href='/topic/new') 새 글 작성